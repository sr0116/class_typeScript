{
  "name": "my-json-server-project",
  "version": "1.0.0",
  "description": "Local mock API server using json-server",
  "main": "server.js",
  "scripts": {
    "json-server": "json-server --watch db.json --routes routes.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.2",
    "json-server": "^0.17.4"
  }
}
